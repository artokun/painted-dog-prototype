import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Reset | Painted Dog",
    description: "Reset",
};

const Page = () => {
    return null;
};

export default Page;
